{
  "name": "screen-number-scanner",
  "version": "1.0.0",
  "author": "prdxqcy",
  "description": "Electron overlay app with a Python OCR scanner for live number tracking.",
  "main": "src/main.js",
  "scripts": {
    "start": "node scripts/start-electron.js",
    "postinstall": "electron-builder install-app-deps",
    "build:worker": "node scripts/build-python-worker.js",
    "package": "npm run build:worker && electron-builder --dir",
    "dist": "npm run build:worker && electron-builder",
    "release": "npm run build:worker && electron-builder --publish always"
  },
  "build": {
    "appId": "com.prdxqcy.ocrscanner",
    "productName": "OCR Scanner",
    "icon": "icon.ico",
    "files": [
      "src/**/*",
      "scripts/**/*",
      "build/icon.ico",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "python/dist/scanner-worker",
        "to": "python-worker"
      }
    ],
    "artifactName": "${productName}-Setup-${version}.${ext}",
    "publish": [
      {
        "provider": "github",
        "owner": "prdxqcy",
        "repo": "OCR-Scanner"
      }
    ],
    "win": {
      "target": "nsis",
      "icon": "icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "perMachine": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  },
  "dependencies": {
    "electron-updater": "^6.3.9"
  },
  "devDependencies": {
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3"
  }
}
